# Derived artifacts. The canonical database is a projection of Arena's shipped
# card database and rebuilds in about a second via `python build_db.py`, so
# there is no reason to carry ~17MB of it in git.
current.db
snapshots/

# Personal data. collection.json holds the player's own card collection, and
# the traffic capture is a raw slice of Player.log.
collection.json
new_traffic.log

# Tooling / session state
.letta/
__pycache__/
*.py[cod]
.venv/
venv/
